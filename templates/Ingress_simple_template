apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name:
  namespace:
  labels:
    key: value
spec:
  rules:
  - http:
      paths:
      - path: /something1
        pathType: Prefix/Exact
        backend:
          service:
            name:
            port:
              number: 